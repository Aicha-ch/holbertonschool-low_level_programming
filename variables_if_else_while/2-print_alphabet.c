#include <stdio.h>
/**
 * main - prints the alphabet in lowercase, followed by a new line
 * return: Always 0 (Success)
 */
int main (void)
{
int i = 0;
for (i >= 'a' && i <= 'z'; i++;)
{
putchar ('i');
putchar ('\n');
}
return (0);
}

